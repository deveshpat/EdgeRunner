# Pin majors so prebuilt wheels (wheels-v1 release) stay valid.
fastapi>=0.110,<1
uvicorn[standard]>=0.27,<1
pydantic>=2,<3
langchain>=0.3,<1
langchain-community>=0.3,<1
langgraph>=0.2,<1
# Prefer prebuilt wheel from GitHub release (see wheels/index.json + bootstrap).
# Source build is a slow fallback only.
llama-cpp-python==0.3.33
huggingface-hub>=0.23,<1
psutil>=5.9,<7
httpx>=0.27,<1
