{
  "servers": [
    {
      "name": "filesystem",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "/tmp/edgerunner-workspace"
      ]
    },
    {
      "name": "git",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-git",
        "--repository",
        "."
      ]
    }
  ],
  "_comment": "Copy to mcp_config.json or set EDGERUNNER_MCP_CONFIG. Requires Node/npx. Builtin tools (shell_exec, python_exec, node_exec, read/write_file) work without this file."
}
